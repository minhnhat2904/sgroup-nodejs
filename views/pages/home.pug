extends ../layout/main.pug

block content 
    h3 Noi bat hom nay
    ul
      each article in articles
        li 
           div 
              p 
                a(href=`/articles/${article.slug}`)
                  strong #{article.title}
              p #{article.content}
              a(href=`/articles/${article._id}/update`) Update
      
    button 
      a(href="/articles/new")  New